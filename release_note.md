# XDL SDK 版本说明

本文档列出本仓库提供的 SDK 版本及客户可见差异。SDK 安装包从发布服务器下载，
不存放在 Git 仓库中。

**SDK 版本总数：1**

| 版本 | 发布状态 | 平台 | 架构 | 主要差异 |
| --- | --- | --- | --- | --- |
| 1.6.7.2 | 公开下载 | Ubuntu/Debian | x86_64、aarch64 | 提供专业客户安装器，包括已安装版本识别、只读环境检查、依赖报告、安全升级、GitHub Release 下载、SHA-256 校验及安装后指导。 |

## 1.6.7.2

### SDK 安装包

- x86_64：`xdl_sdk_1.6.7.2_x86_64_debian.run`
- aarch64：`xdl_sdk_1.6.7.2_aarch64_debian.run`

### 安装行为

- 自动识别主机架构，也可使用 `--arch x86_64|aarch64` 手动指定。
- 支持使用 Debian 包管理体系的 Ubuntu 和 Debian。
- 提供五阶段终端流程：系统兼容性、依赖项、下载条件、安装计划和安装包准备。
- 安装前检查 `curl`、`ca-certificates`、`coreutils`、`libc6`、`libstdc++6`、
  `dctrl-tools`、`build-essential`、`dkms` 及当前内核头文件。
- Debian 包数据库存在未完成或损坏的软件包时停止安装。
- 安装前识别现有 SDK：相同版本跳过重复安装，并显示可选的手动卸载和重新安装命令；旧版本经确认后同步卸载并验证完成，
  再安装新版本；新版本或版本未知的现有安装会阻止覆盖，并显示手动卸载指导。
- 检查可用磁盘空间、代理配置和 SDK 下载源可达性。
- 执行安装包前校验发布文件的字节数和 SHA-256。
- 支持 `--check-only`、`--yes`、`--download-only`、`--download-dir` 和
  `--no-color`。当前 SDK 不支持跳过驱动安装。
- 安装结束后显示软件包检查、重启、设备验证和卸载指导。
- 只读预检发现缺失包时，自动生成可直接复制的 APT 安装命令和复检命令；包数据库、
  磁盘空间及网络异常也会显示对应处理建议。
- 下载的 `.run` 文件保存在 Git 忽略的 `downloads/` 目录或客户指定目录中。

### 客户文档

- 重新设计中英文客户首页，提供三步快速安装、支持范围、前置条件矩阵、常见部署
  场景、安装后验证、异常处理、安全说明和支持信息。
- 明确区分已有现场验证记录与仍需在目标系统验证的兼容性声明。
- 增加中英文 FAQ，覆盖 Debian 包状态、驱动生效、手动加载模块、MPS 模式申请、
  卸载残留、HTTP `403` 和内核头文件。MPS 切换包不随
  客户交付提供，需要联系 XDL 技术支持。
- 增加 RK3588/ARM 驱动编译异常指导，覆盖厂商内核 Header 安装、缺少 `python`
  命令及缺少 `scripts/basic/fixdep`；要求 Header 与板卡镜像、运行内核和架构匹配。
- 中文和英文版本说明独立呈现，确保客户页面不会跳转到另一种语言。

### 验证状态与已知限制

截至 2026-08-26，两个 GitHub Release 地址均已通过匿名 Range 下载、整包下载及
SHA-256 校验。x86_64 环境已完成安装和重启验证，`ae-smi` 可正常识别 RPP_R9 设备，
设备处于 `WORKING` 状态。aarch64 目标安装、驱动加载以及两种架构的卸载流程仍需验证。
